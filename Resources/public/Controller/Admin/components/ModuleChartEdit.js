import React from "react";
import {Link} from "react-router";

export default class ModuleTableEdit extends React.Component {
  render(){
    return (
      <div class="well" onClick={this.onClick}>
      
      </div>
    );
  }
}